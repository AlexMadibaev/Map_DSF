# Прогулка по GLB-карте

## Запуск

Самый простой способ — дважды нажать `Запустить карту.cmd`.

Или через терминал:

1. Установите Node.js 20.19+.
2. В этой папке выполните `npm install`.
3. Запустите `npm run dev`.
4. Откройте адрес, показанный в терминале (обычно `http://localhost:5173`).

Приложение использует точную Meshopt-версию карты. Для совместимости с лимитами Vercel файл разделён на `public/map.glb.part1` и `public/map.glb.part2`; браузер автоматически объединяет их при загрузке.

Meshopt-версия уменьшена с 422,59 МБ до 109,67 МБ. Все 8 879 969 треугольников и 325 исходных сеток сохранены. Положение, масштаб, материалы и композиция сцены не упрощались.

На Vercel части карты и собранные assets кэшируются браузером на один год. HTML всегда проверяется на обновления. При замене модели нужно увеличить параметр `?v=` в `src/main.js`.

Управление: WASD или стрелки — движение, мышь — обзор, пробел/Q — выше, Ctrl/E — ниже, Shift — ускорение, R — возврат на старт, Esc — освободить мышь.

Для модели на 1,8 млн треугольников используется облегчённый режим перемещения без предварительного построения тяжёлой физической сетки.
